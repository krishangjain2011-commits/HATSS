# Copy this file to .env before starting Docker Compose.
# This file is safe to commit; replace the password with a unique local value.

# PostgreSQL
POSTGRES_DB=hatss
POSTGRES_USER=hatss
POSTGRES_PASSWORD=replace-with-a-strong-local-password
POSTGRES_PORT=5432

# Application ports exposed to the host
BACKEND_PORT=8000
FRONTEND_PORT=5173

# FastAPI runtime configuration
HATSS_ENVIRONMENT=development
HATSS_DEBUG=false
HATSS_DOCS_ENABLED=true
HATSS_DATABASE_HOST=localhost
HATSS_DATABASE_PORT=5432
HATSS_CORS_ORIGINS=["http://localhost:5173"]
HATSS_TRUSTED_HOSTS=["localhost","127.0.0.1","testserver","backend"]

# Optional local AI evidence briefing. Requires Ollama running locally with the configured model.
HATSS_COPILOT_ENABLED=false
HATSS_COPILOT_BASE_URL=http://127.0.0.1:11434
HATSS_COPILOT_MODEL=llama3.2:3b
# First-time model loading can take a while on CPU-only systems.
HATSS_COPILOT_TIMEOUT_SECONDS=120
